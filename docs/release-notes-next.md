# 下个版本发布说明（待发布）

- 官方插件改为按宿主版本从 GitHub Pages 下载。Profile 必需插件继续随包提供，其余插件不再进入 APK 或 Web 宿主静态资源。
- 商店与外部 Manifest URL 安装保留；已安装插件可离线恢复。网络不可用时仍可使用必需功能，不能安装新可选插件。
- 官方插件继续与应用统一发版；应用升级时更新已安装官方插件，失败时保留可用缓存及插件数据。
- 原生能力和服务端功能仍由发行版本决定，下载插件不会新增原生模块或部署服务端代码。
- 维护者须以新版本首次发布迁移，不重建旧标签；开发数据不兼容时手动清空，不自动迁移或删除。

发布前将这些内容纳入实际新版本记录，确认 Pages 插件目录上线并完成浏览器与 Android WebView 下载验收，再发布 APK。
