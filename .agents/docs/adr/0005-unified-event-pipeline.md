# ADR 0005：统一事件广播

- 日期：2026-08-20

## 决策

`ChronosEngine.events` 是强类型 `EventPipeline` 广播入口。插件和宿主通过 `emit`、`on` 通信。动作执行和数据转换由所属模块负责。

## 演进与取舍

事件总线和数据管道最初还提供 `serial`、`waterfall` 两种拦截机制。生产代码没有使用它们，因此已在 2026-09 移除。项目不再保留 `engine.pipeline` 或动作守卫包装层。事件广播继续负责状态通知，项目不为尚未出现的扩展需求维护拦截链。
